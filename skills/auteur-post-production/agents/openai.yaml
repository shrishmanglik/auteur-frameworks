interface:
  display_name: "AUTEUR Post-production"
  short_description: "Build and audit evidence-bound spoken edits"
  default_prompt: "Use $auteur-post-production to plan and audit this spoken A-roll edit from source clips through final human review."
policy:
  allow_implicit_invocation: true
